C-> create -> inserir dados no banco // -> INSERT INTO
R-> read -> consultar dados no banco // -> SELECT
U-> update -> atualizar os dados no banco // -> UPDATE
D-> delete -> apagar dados no banco // -> DELETE FROM

 -No MYSQL workbench:

CREATE DATABASE escola;
USE escola;

CREATE TABLE alunos(
	id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(100)NOT NULL,
    email VARCHAR(100)NOT NULL
    );
    